int main() [

$* это многострочный комментарий который записан в одну строку *$

$*
так же этот
комментарий 
может быть записан
к несколько
строк


и могут быть пустые
*$


$*
это не разрешено печатать (комментарий)
output("VPN");
а это разрешено печатать:
*$
output("MAX"); $ должен это напечатать
]